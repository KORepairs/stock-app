{
  "name": "stock-app",
  "version": "1.0.0",
  "type": "module",
  "main": "src/index.js",
  "description": "",
  "scripts": {
    "dev": "nodemon src/index.js",
    "start": "node src/index.js",
    "backup": "mkdir -p backups && sqlite3 data.db \".backup 'backups/db-$(date +%F-%H%M%S).sqlite3'\"",
    "rebuild:sqlite": "npm rebuild better-sqlite3 --build-from-source",
    "postinstall": "npm run rebuild:sqlite"
  },
  "dependencies": {
    "basic-auth": "^2.0.1",
    "cors": "^2.8.5",
    "dotenv": "^17.2.1",
    "express": "^5.1.0",
    "pg": "^8.16.3"
  },
  "devDependencies": {
    "nodemon": "^3.1.10"
  },
  "engines": {
    "node": "20.x"
  },
  "license": "ISC",
  "author": ""
}
